body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: #111;
  color: #fff;
}
.navbar {
  display: flex;
  justify-content: space-between;
  background: #000;
  padding: 10px 20px;
}
.hero {
  background-image: url('https://wallpaperaccess.com/full/2703652.png');
  background-size: cover;
  background-position: center;
  height: 80vh;
  position: relative;
}

.hero-content {
  position: absolute;
  bottom: 50px;
  left: 30px;
  max-width: 500px;
}

.hero-content h1 {
  font-size: 3rem;
}



.hero-content p {
  margin: 20px 0;
  font-size: 1rem;
}
.logo { font-size: 24px; color: red; }
.nav-right { display: flex; align-items: center; gap: 10px; }
.search-input { padding: 5px 10px; border-radius: 4px; border: none; }
.toggle-btn { cursor: pointer; background: none; border: none; font-size: 18px; color: #fff; }
.row { margin: 20px; }
.row h2 { margin-left: 10px; }
.row-posters {
  display: flex;
  overflow-x: auto;
  gap: 16px;
  padding: 10px;
  
}

.poster { width: 140px; border-radius: 8px; display: block; }
.poster-title { text-align: center; font-size: 0.9rem; margin-top: 6px; }
.description { text-align: center; font-size: 0.8rem; margin: 4px 0; }
.play-btn {
  display: block;
  margin: 0 auto 10px;
  background: #e50914;
  color: #fff;
  border: none;
  padding: 6px 12px;
  border-radius: 4px;
  cursor: pointer;
}

body.light {
  background: #eee;
  color: #000;
}
.modal {
  display: none;
  position: fixed;
  z-index: 100;
  left: 0; top: 0;
  width: 100%; height: 100%;
  background-color: rgba(0,0,0,0.8);
  justify-content: center;
  align-items: center;
}
.modal-content {
  position: relative;
  width: 80%;
  max-width: 800px;
  background: #000;
  padding: 20px;
}
.close-btn {
  position: absolute;
  top: 10px; right: 20px;
  font-size: 24px;
  color: #fff;
  cursor: pointer;
}
.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: #141414;
  color: white;
  padding: 10px 50px;
  
  position: sticky;
  top: 0;
  z-index: 999;
}

.movie-card:hover img.poster {
  opacity: 0.8;
}
/* .movie-card .play-btn {
  margin-top: 10px;
  background-color: #e50914;
  border: none;
  padding: 8px 16px;
  color: white;
  border-radius: 5px;
  font-size: 1em;
  cursor: pointer;
  transition: background-color 0.3s;
} */
/* .movie-card:hover .overlay {
  transform: translateY(0%);
} */
 /* .movie-card img.poster {
  width: 100%;
  height: auto;
  border-radius: 10px;
  transition: opacity 0.3s ease;
} */
 .movie-card {
  position: relative;
 overflow: hidden;
  border-radius: 10px;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  cursor: pointer;
}
/* .row-posters:hover {
  animation-play-state: paused;
} */
.auth-buttons{
  display: flex;
  
}

.auth-buttons button {
  background-color: #ff004f;
  color: #fff;
  display: flex;
  border: none;
  padding: 8px 16px;
  margin: 5px;
  border-radius: 4px;
  cursor: pointer;
  font-weight: bold;
}

.modal {
  display: none;
  position: fixed;
  z-index: 999;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background-color: rgba(0, 0, 0, 0.8);
  justify-content: center;
  align-items: center;
}

.modal-content {
  background-color: #1c1c1c;
  padding: 30px;
  border-radius: 8px;
  width: 500px;
  color: #fff;
  text-align: center;
  position: relative;
}

.modal-content input {
  width: 100%;
  margin: 10px 0;
  padding: 10px;
  border: none;
  border-radius: 4px;
}

.modal-content button {
  background-color: #ff004f;
  color: #fff;
  padding: 10px;
  width: 100%;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

.modal-content .close {
  position: absolute;
  right: 15px;
  top: 10px;
  font-size: 24px;
  cursor: pointer;
}



.movie-card {
  flex: 0 0 auto;
  width: 200px; /* or your desired card width */
}

 .movie-card:hover {
  transform: scale(1.05);
  box-shadow: 0 12px 25px rgba(0, 0, 0, 0.6);
  z-index: 2;
}
.movie-card:hover .overlay {
  transform: translateY(0%);
}
.movie-card .overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: rgba(0, 0, 0, 0.8);
  color: #fff;
  padding: 10px;
  transform: translateY(100%);
  transition: transform 0.4s ease-in-out;
}
.movie-card .play-btn {
  margin-top: 10px;
  background-color: #e50914;
  border: none;
  padding: 8px 16px;
  color: white;
  border-radius: 5px;
  font-size: 1em;
  cursor: pointer;
  transition: background-color 0.3s;
}
.movie-card .play-btn:hover {
  background-color: #b00610;
}
.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: #141414;
  color: white;
  padding: 10px 10px;
  gap: 30%;
  position: sticky;
  top: 0;
  z-index: 999;
}
.logo {
  font-size: 1.8em;
  font-weight: bold;
  color: #e50914;
}

.nav-links {
  display: flex;
  gap: 20px;
  
}

.nav-links a {
  color: white;
  text-decoration: none;
  font-weight: 500;
  transition: color 0.3s;
}

.nav-links a:hover {
  color: #e50914;
}

.nav-right {
  display: flex;
  margin-right: 30%;
  align-items: left;
  gap: 10px;
}
.site-footer {
  background-color: #111;
  color: #aaa;
  padding: 30px 0 20px;
  font-size: 14px;
  text-align: center;
  border-top: 1px solid #333;
}

.footer-container {
  max-width: 1000px;
  margin: 0 auto;
}

.footer-links {
  margin-bottom: 15px;
}

.footer-links a {
  color: #aaa;
  margin: 0 15px;
  text-decoration: none;
  transition: color 0.3s ease;
}

.footer-links a:hover {
  color: #fff;
}

.footer-text {
  font-size: 13px;
  color: #777;
}
.auth-buttons button {
  margin-left: 40%;
  padding: 5px 10px;
 
  background: #00bfff;
  border: none;
  cursor: pointer;
  color: white;
  font-weight: bold;
}


body.light .navbar { background: #ddd; }
body.light .logo { color: darkred; }
body.light .toggle-btn { color: #000; } 
